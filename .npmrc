# Jest chạy ESM thật (mã nguồn là "type": "module") nên cần cờ này của Node.
# Để ở .npmrc để `npx jest` trần — đúng lệnh nghiệm thu trong hợp đồng — chạy được ngay,
# không phải nhớ đặt NODE_OPTIONS.
node-options=--experimental-vm-modules
